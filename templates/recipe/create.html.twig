{% extends 'base.html.twig' %}

{% block title %} StoreFreeze {% endblock %}

{% block body_id %}app{% endblock %}

{% block header %}

{% endblock %}

{% block main %}
    {% include 'baseOpen.html.php' %}

<div class="mdl-tabs__tab-bar">
    <a class="mdl-navigation__link mdl-tabs__tab" href="createRecette">Ajouter Recette</a>
    <a class="mdl-navigation__link mdl-tabs__tab" href="readRecette">Voir Recette</a>
</div>

<section class="paragraphe subtitle backgroundCreate">
    <div class="black">
        <h3>Créer recette</h3>
    </div>
    <div>
        {{ form_start(form, {
            attr: {
                novalidate : "novalidate",
                class: "row"
            }
        }) }}

        {{ form_start(form2, {
            attr: {
                novalidate: "novalidate",
                class: "row"
            }
        }) }}

        <div class="border col-12 col-lg6">
            <table>
                <br>
                <tr>
                    <td>                    
                    {{ form_row(form.name) }}
                    </td>
                    <td>
                    {{ form_row(form.CategoryRecette) }}
                    </td>
                </tr>
                <tr>
                    <td>
                    {{ form_row(form.Book) }}
                    </td>
                    <td>
                    {{ form_row(form.Page) }}
                    </td>
                </tr>
            </table>
                <td>
                <br>
                <hr>
                <br>                
                {{ form_row(form.NumberPeople) }}
                <br>
                </td>

            <table>
                <tr>
                    <td>
                    {{ form_row(form2.name) }}
                    {# {{ form_row(form.productRecettes) }} #}
                    </td>
                    <td>
                    {{ form_row(form.quantityGram) }}
                    </td>
                    <td>
                    {{ form_row(form.quantityUnit) }}
                    </td>
                </tr>
            </table>
            {# <table>
                <tr>
                    <td>
                    {{ form_row(form.productRecettes) }}
                    </td>
                    <td>
                    {{ form_row(form.quantityGram) }}
                    </td>
                    <td>
                    {{ form_row(form.quantityUnit) }}
                    </td>
                </tr>
            </table> #}
            
            <br>
            <hr>
            <br>
            {# {{ form_row(form.numberPeople) }} #}
            {# <br> #}
            {# {{ form_row(form.plat) }} #}
            {# <br> #}
            {# {{ form_row(form.book) }} #}
            {# <br> #}
            {# {{ form_row(form.page) }} #}
            {# <br> #}
            {{ form_row(form.timePrepa) }}
            <br>
            {{ form_row(form.timeCook) }}
            <br>
            <hr>
            <br>
            {{ form_row(form.description) }}
            <br>
            <button class="mdl-button mdl-js-button mdl-button--raised mdl-button--myColorValidate" type="submit">Valider</button>
            <br>
            {{ form_end(form) }}
            {{ form_end(form2)}}
            <br>
        </div>
        <br>
    </div>
    {% include 'baseClose.html.php' %}
<section>
{% endblock %}
