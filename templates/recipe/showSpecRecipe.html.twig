{% extends "base.html.twig" %}

{% block title %}StoreFreezer{% endblock %}

{% block body_id %}app{% endblock %}

{% block header %}{% endblock %}

{% block main %}
    {% include 'baseOpen.html.php' %}

    <div class="mdl-tabs__tab-bar">
        <a class="mdl-navigation__link mdl-tabs__tab" href="createRecette">Ajouter Recette</a>
        <a class="mdl-navigation__link mdl-tabs__tab" href="readRecette">Voir Recette</a>
    </div>
    {% for recette in recettes %}
    <section class="paragraphe subtitle backgroundCreate">
        <div class="black">
            {# <h3>Recette {{ recette.name }}</h3> #}
        </div>

        <div class="black">
            <div>
                <table class="tableShowJob backgroundWhite">
                    <tbody>
                    <br><br>
                        <tr class="borderTrue">                      
                            {# <th>{{ recette.pictureRecette }}</th>       #}
                            <th colspan='4' class="borderTrue" width="120%"><h3><u>{{ recette.name }}</u></h3></th>                            
                            {# <th colspan='1' class="borderTrue">Plat</th> #}
                        </tr>
                        <tr class="borderTrue">                            
                            <th class="borderTrue">Nombre <br> de <br> personne</th>
                            <th colspan='1' class="borderTrue">Ingredients</th>
                            <th class="borderTrue">Temps</th>
                            <th class="borderTrue">Livres</th>
                        </tr>
                        <tr class="borderTrue">
                            <td rowspan='1' class="borderTrue">
                            {% for recette in recettes %}
                                {# {% if recette.id == productRecette.recette.id %} #}
                                    {# <li>{{ productRecette.product.name }}</li><br> #}
                                {# {% endif %}                                 #}
                                <td></td>
                            {% endfor %}
                            
                            </td>
                            {# </tr> #}

                            {# <tr>
                                <th>Nombre de personne</th>
                                <th>Temps</th>
                                <th>Livres</th>
                            </tr> #}
                            {# <tr> #}
                                {# <td>{{ recette.numberPeople }}</td> #}
                            <td class="borderTrue"><b> Temps de préparation : </b>{{ recette.timePrepa|date('H:i:s') }}
                                <br>
                                    <b> Temps de cuisson : </b>{{ recette.timeCook|date('H:i:s') }}
                            </td>
                            <td class="borderTrue">{{ recette.book.title }}
                                <br> 
                                    <b>Couleur : </b>{{ recette.book.color }} 
                                <br> 
                                    {# <b>Page</b> N°{{ recette.page }}</td> #}
                            </td>
                        </tr>

                        <tr class="borderTrue">
                            <th colspan='1' class="borderTrue">Plat</th>
                            <th  colspan='3' class="borderTrue">Description</th>
                        </tr>
                        <tr class="borderTrue">
                            <td colspan='1' class="borderTrue">{{ recette.CategoryRecette.name }}</td>
                            <td colspan='3' class="borderTrue">{{ recette.description|nl2br }}</td>
                        </tr>

                    </tbody>
                </table>
            </div>
            <br><br><br><br>
        </div>
    {% include 'baseClose.html.php' %}
    </section>
    {% endfor %}
{% endblock %}